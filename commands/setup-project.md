---
description: Bootstraps or audits evidence-backed OpenCode governance for a Node or TypeScript project.
agent: orchestrator
---

Load `setup-node-project` and bootstrap or audit the current repository using only verified evidence: $ARGUMENTS

Inspect the repository and authenticated GitHub context, then print the readiness report.
Unless the request is report-only or excludes an asset, create missing applicable assets
and preservation-first merge incomplete assets directly. Preserve existing project settings,
comments, MCP configuration, and unrelated work.
Delegate only when it is useful; setup does not require an issue, plan, digest, or
formal handoff. Validate the result with the repository's real commands. Never commit,
push, create a PR, or change remote settings unless explicitly asked.
